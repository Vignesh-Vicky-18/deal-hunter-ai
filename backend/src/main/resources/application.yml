spring:
  application:
    name: deal-hunter-ai
  datasource:
    url: jdbc:postgresql://localhost:5432/dealhunter
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
